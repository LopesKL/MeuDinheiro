import { Switch } from 'antd';

/**
 * Componente base de toggle switch usando Ant Design
 */
const BaseToggleSwitchInput = ({ ...props }) => {
  return <Switch {...props} />;
};

export default BaseToggleSwitchInput;
