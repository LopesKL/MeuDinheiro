import { Switch } from 'antd';

/**
 * Componente base de switch condicional
 */
const BaseConditionalSwitchInput = ({ ...props }) => {
  return <Switch {...props} />;
};

export default BaseConditionalSwitchInput;
